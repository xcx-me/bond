const {getConcreteCookie} = require('app/util/ajax/ajax')

App({
	onLaunch: function () {
		// getConcreteCookie(() => {
		// 	console.log('EVERYTHING OK!!!')
		// })
	},

	globalData: {

	},

	isLocalhost: true
})